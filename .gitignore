node_modules/
.env
.DS_Store
data/*.db
data/*.db-shm
data/*.db-wal

# Personal data — each user provides their own (copy from *.example)
config/profile.json

# Custom AI prompt overrides (defaults live in src/prompts.js)
config/prompts.json

# Setup wizard state + debug-mode sandbox (per-user, never shipped)
config/setup-state.json
data/setup-debug/

# Operator (SaaS) sandbox — isolated test instance DB (npm run gui:operator)
data/operator-sandbox/

# Local-only README screenshot helpers + their throwaway demo data (not shipped)
scripts/seed-demo.mjs
scripts/shoot-demo.mjs
data/demo/

# Database backups (daily/manual/upload/pre-import snapshots)
data/backups/

# Generated exports + Excel lock files
data/*.xlsx
data/~$*

# Crawlee storage
storage/

# Run logs
logs/
